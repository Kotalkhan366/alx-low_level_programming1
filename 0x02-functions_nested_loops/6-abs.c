#include <stdio.h>
#include <stdlib.h>
/**
 * main - A program that computes the value of an interger.
 *
 * Return: Always 0.
 * */
int main()
{
int num = -42;
int absValue = abs(num);
printf("Absolute value of %d is %d\n", num, absValue);
return 0;
}
