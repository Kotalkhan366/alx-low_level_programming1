#include <stdio.h>
/**
 * main - A program that adds two intergers and returns the result.
 *
 * Return: Always 0.
 * */
int addIntergers(int a, int b)
{
return a + b;
}
int main()
{
int num1 = 10;
int num2 = 20;
int sum = add interers(num1, num2);
printf("The sum of %d and %d is &d\n", num1, num2, sum);
return 0;
}
